# Rust
/target
**/*.rs.bk
*.pdb

# Built frontend (regenerated by `make frontend`; embedded at build time)
/frontend/dist
/frontend/node_modules
/frontend/coverage

# Local databases
*.db
*.db-shm
*.db-wal

# Downloaded engines / weights
/engines

# Env / editor
.env
.DS_Store
